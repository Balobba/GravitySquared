/*
 * A file for game constants
 * all uppercase and use underscore as delimiter
 */

var GRAVITY = 2000;
var HUD_WIDTH = 90;
var BLOCK_SIZE = 32;
var SPAWN_OFFSET = 200;

var MAX_PLAYERS = 4;
var MIN_PLAYERS = 2;
var ANGLES = [0, 90, 180, 270];
